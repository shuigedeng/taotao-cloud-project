archivesBaseName = 'taotao-cloud-phoenix'

jar.enabled = false
bootJar.enabled = false

dependencies {
    implementation 'org.projectlombok:lombok'
    annotationProcessor "org.projectlombok:lombok:${lombokVersion}"
}
