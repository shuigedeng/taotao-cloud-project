apiVersion: v1
kind: ConfigMap
metadata:
  name: taotao-cloud-oauth2-server-configmap
data:
  APPLICATION_NAME: taotao-cloud-oauth2-server
  APPLICATION_VERSION: 20201.9.3
---
apiVersion: v1
kind: Secret
metadata:
  name: taotao-cloud-oauth2-server-secret
type: Opaque
data:
  ADMIN_USERNAME: YWRtaW4=
  ADMIN_PASSWORD: MTIzNDU2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taotao-cloud-oauth2-server
  labels:
    app: taotao-cloud-oauth2-server
spec:
  replicas: 1
  minReadySeconds: 10
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: taotao-cloud-oauth2-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 30%
      maxUnavailable: 30%
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: taotao-cloud-oauth2-server
    spec:
      terminationGracePeriodSeconds: 30
      schedulerName: default-scheduler
      restartPolicy: Always
      dnsPolicy: ClusterFirst
      securityContext: { }
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/arch
                    operator: In
                    values:
                      - amd64
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: "kubernetes.io/hostname"
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - ops-nginx-api
      volumes:
        - name: taotao-cloud-oauth2-server-hostpath
          hostPath:
            path: /tmp/logs/taotao-cloud-oauth2-server
            type: Directory
        - name: taotao-cloud-oauth2-server-secret
          secret:
            secretName: taotao-cloud-oauth2-server-secret
      containers:
        - name: taotao-cloud-oauth2-server
          stdin: true
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          tty: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { }
            privileged: false
            readOnlyRootFilesystem: false
            runAsNonRoot: false
          image: registry.cn-hangzhou.aliyuncs.com/taotao-cloud-project/taotao-cloud-oauth2-server:2021.9.3
          imagePullPolicy: IfNotPresent
          ports:
            - name: oauth2-port
              containerPort: 33336
          volumeMounts:
            - name: taotao-cloud-oauth2-server-hostpath
              mountPath: /root/logs
            - name: taotao-cloud-oauth2-server-secret
              mountPath: /etc/secret
              readOnly: true
          env:
            - name: APPLICATION_NAME
              valueFrom:
                configMapKeyRef:
                  name: taotao-cloud-oauth2-server-configmap
                  key: APPLICATION_NAME
            - name: APPLICATION_VERSION
              valueFrom:
                configMapKeyRef:
                  name: taotao-cloud-oauth2-server-configmap
                  key: APPLICATION_VERSION
            - name: ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: taotao-cloud-oauth2-server-secret
                  key: ADMIN_USERNAME
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: taotao-cloud-oauth2-server-secret
                  key: ADMIN_PASSWORD
          resources:
            requests:
              cpu: 1
              memory: 512Mi
            limits:
              cpu: 2
              memory: 2048Mi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 33336
              scheme: HTTP
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 33336
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 5
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - echo 'taotao cloud admin postStart success' >> /root/postStart
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - sleep 30
---
apiVersion: v1
kind: Service
metadata:
  name: taotao-cloud-oauth2-server
  labels:
    app: taotao-cloud-oauth2-server
spec:
  type: NodePort
  selector:
    app: taotao-cloud-oauth2-server
  ports:
    - name: oauth2-port
      protocol: TCP
      port: 33336
      targetPort: 33336
      nodePort: 33336
